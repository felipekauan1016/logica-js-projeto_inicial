//crie uma funçao que ao clicar ele conta quantas vezes o usuario clicou.

// Inicializa o contador com zero
let contador = 0; 

// Incrementa o contador a cada clique
function clicou() {
  contador++; 
  alert (`voce clicou em mim ${contador} vezes`)
}